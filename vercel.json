{
  "outputDirectory": "dist",
  "installCommand": "npm install && npm install --prefix User && npm install --prefix Admin",
  "buildCommand": "npm run sync-env && npm run build --prefix User && npm run build --prefix Admin && node scripts/assemble-dist.js",
  "rewrites": [
    { "source": "/admin", "destination": "/admin/index.html" },
    { "source": "/admin/(.*)", "destination": "/admin/index.html" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
